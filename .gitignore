# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# .env ต้องอยู่ใน git — Vercel build อ่าน Firebase config จากไฟล์นี้
# (Firebase web config ไม่ใช่ความลับ — ฝังใน bundle อยู่แล้ว)
# ถ้าย้ายไปตั้ง env vars ใน Vercel Dashboard แล้ว ค่อยลบ .env ออกจาก git ได้
# ลับจริง (เช่น service-account json) ห้าม commit — ใส่ .env.secret แทน
.env.secret
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.codex-artifacts/

# Course material downloads (do not commit large binaries)
*.zip
วิทยาศาสตร์และเทคโนโลยี*/
