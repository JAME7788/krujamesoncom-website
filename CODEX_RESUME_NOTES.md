# บันทึกงานค้างสำหรับทำต่อ

อัปเดตล่าสุด: จัดการความเสี่ยงลิขสิทธิ์สื่อ/สไลด์เดิม ถอด public/slides ออกจากเว็บ และเพิ่มแนวทางเรียนด้วยตนเองให้หน้าบทเรียนแล้ว

## ทำไปแล้ว

- เพิ่มคอร์ส `อิเล็กทรอนิกส์เบื้องต้น (ฉบับครูเจมส์)` เป็น `electronics-basic`
- เปลี่ยนลิงก์คอร์ส electronics ให้เปิดบทเรียนในเว็บของเราเอง แทนการเปิด PDF ต้นฉบับ
- ลบไฟล์ `public/files/nuecamp/NUeCampEBook.pdf` ออกจาก public แล้ว
- เพิ่มคู่มือคอร์สที่เขียนใหม่เอง:
  - `public/worksheets/electronics/electronics-course-outline.html`
- เปลี่ยนข้อความหน้า Admin จาก “จาก PPT อจท.” เป็น “เรียบเรียงใหม่ตามตัวชี้วัด”
- ปรับ `src/pages/UnitDetail.tsx` ให้ไม่แสดงภาพสไลด์ third-party เดิม ถ้ามี `slideImages` ให้ใช้สไลด์ที่ระบบสร้างเองแทน
- รัน `npm.cmd run build` ผ่านแล้วหลังแก้คอร์ส electronics
- ลบโฟลเดอร์ `public/slides` ออกจากเว็บแล้ว
- ล้าง reference `/slides/...`, `อจท`, `Aksorn` ออกจาก `src`, `public`, และ `dist` แล้ว
- เพิ่มสไลด์ “เรียนไวใน 3 นาที” ให้ชุดสไลด์ที่สร้างใหม่ เพื่อให้เด็กจับประเด็นเร็วขึ้น
- เพิ่มสไลด์ “แนวทางศึกษาด้วยตนเอง”, “สาระสำคัญของบทเรียน”, และ “ตรวจความเข้าใจด้วยตนเอง” ในชุดสไลด์ที่ระบบสร้างจากตัวชี้วัด
- เพิ่มกล่อง “วิธีเรียนหน่วยนี้ให้เข้าใจ” ในหน้าภาพรวมบทเรียน ให้เด็กเรียนเองตามขั้น อ่านเป้าหมาย → จับใจความสำคัญ → ลงมือฝึก → ตรวจความเข้าใจ
- ปรับ responsive หน้า Unit Detail เพื่อลดข้อความล้นและแนวนอนเลื่อนบนจอแคบ
- รัน `npm.cmd run build` ผ่านแล้วหลังปรับเนื้อหาและหน้าจอบทเรียน

## งานที่ต้องทำต่อ

- ตรวจหน้าจอบทเรียนจริงในแต่ละชั้น แล้วค่อย ๆ ปรับคำอธิบาย/กิจกรรมของหน่วยที่ยังดูทั่วไปเกินไปให้เป็นภาษาเด็กมากขึ้น
- ถ้าต้องการสไลด์เฉพาะหน่วยแบบละเอียดกว่านี้ ให้เพิ่มเป็น `richSlides` หรือเพิ่ม `lessonNotes` ใน `unitExtras`

## คำสั่งตรวจหลังทำต่อ

```powershell
rg -n "อจท|Aksorn|aksorn|PPT อจท|Auto-generated from PPT|/slides/|slides/cs_|slides/dt_" src public -g "!*node_modules*"
npm.cmd run build
```

## หมายเหตุ

- หลังล้าง `public/slides` แล้ว เว็บควรใช้สไลด์ที่สร้างจากตัวชี้วัด/เนื้อหาในระบบเอง
- Dev server เปิดที่ `http://127.0.0.1:5173/`
